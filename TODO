Compress pastes?
  lz-string (takes 1s for 2MB -- too long)

prevent just anyone from using the api

find the base cljs size; try to approach that

make paste button more visible when there's a valid paste
  scan left to right ; it's the last thing

integrity check with hash
  prevent tampering

code signing

continuous
  functional tests
  validation of html and css
  validation of ssl grade

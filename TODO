Compress pastes?
  lz-string (takes 1s for 2MB -- too long)

tool to encrypt about/donate posts given a key

remote-addr used in log is wrong, due to the reverse proxy

prevent just anyone from using the api?

rate limiting

find the base cljs size; try to approach that

have search engines not index pastes

add a new line to each paste?

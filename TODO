Compress pastes?
  lz-string (takes 1s for 2MB -- too long)

optional integrity check with hash
  prevent tampering

load js last to speed up initial render

code signing

continuous
  functional tests
  validation of html and css
  validation of ssl grade

Compress pastes?
  lz-string (takes 1s for 2MB -- too long)

prevent just anyone from using the api

rate limiting
  mention in about

find the base cljs size; try to approach that

allow forking of posts

check ip tables in server
  return error code for rate limiting

integrity check with hash
  prevent tampering

continuous
  functional tests
  validation of html and css
